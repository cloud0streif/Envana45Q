[build]
builder = "NIXPACKS"
buildCommand = "cd backend && poetry install --no-dev"

[deploy]
startCommand = "cd backend && poetry run uvicorn src.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
