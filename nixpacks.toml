[phases.setup]
nixPkgs = ["python311", "poetry"]

[phases.install]
cmds = ["cd backend && poetry install --no-dev --no-root"]

[start]
cmd = "cd backend && poetry run uvicorn src.main:app --host 0.0.0.0 --port ${PORT:-8000}"
